{
	"name": "@xernerx/terminal",
	"version": "1.0.2",
	"description": "",
	"main": "dist/main.js",
	"scripts": {
		"beta": "npm version prerelease --preid=beta",
		"patch": "npm version patch",
		"minor": "npm version minor",
		"major": "npm version major",
		"docs": "npx typedoc",
		"build": "npm i && npx tsup && npx @arethetypeswrong/cli --pack && npm run docs",
		"dev": "npm i && npm link && npx tsup --watch",
		"test": "npm install --engine-strict=true",
		"pub": "git add . && git commit -m \"type update\" && npm run patch && git push origin patch"
	},
	"keywords": [],
	"author": "Xernerx Studios",
	"license": "MIT",
	"packageManager": "pnpm@10.32.0",
	"devDependencies": {
		"@types/chalk-animation": "^1.6.3",
		"@types/node": "^24",
		"prettier": "^3.8.3",
		"tsup": "^8.5.1",
		"typescript": "^6.0.3"
	},
	"dependencies": {
		"boxen": "^8.0.1",
		"chalk": "^5.6.2",
		"ora": "^9.4.0"
	}
}
